[buildout]

parts += 
        apache_rh

[apache_rh]
recipe = collective.recipe.cmd
on_install = true
on_update = true
cmds =
    if [ -f /etc/redhat-release ] ; then
        sudo yum -y install \
            httpd
    fi

