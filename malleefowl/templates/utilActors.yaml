- id: StatusWriter
  type: GroovyActor
  properties:
    step: |
      new File(filename).withWriterAppend{ out -> out.println message }
    inputs:
      filename:
        type: String
      message:
        type: String