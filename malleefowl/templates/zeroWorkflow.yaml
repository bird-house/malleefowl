imports:

- classpath:/common/directors.yaml
- classpath:/common/groovy/actors.yaml

components:

# actors

<%include file="wpsActors.yaml"/>

# workflow

- id: ZeroWorkflow
  type: Workflow
  properties:
    director: !ref MTDataDrivenDirector
    nodes:
    - !ref DownloadData
    - !ref ShowDownload
    - !ref RunProcess
    - !ref RenderResult

- id: DownloadData
  type: Node
  properties:
    actor: !ref ConstantSource
    constants:
      value: 
        - http://localhost:8090/files/test.nc
    outflows:
      value: /download/source/

- id: ShowDownload
  type: Node
  properties:
    actor: !ref PrintStreamWriter
    inflows:
      message: /download/source/

- id: RunProcess
  type: Node
  properties:
    actor: !ref WpsExecute
    constants:
      service: ${service}
      identifier: ${identifier}
      input: ${input}
      output: ${output}
    inflows:
      source: /download/source/
    outflows:
      result: /wps/result/  
 
- id: RenderResult
  type: Node
  properties:
    actor: !ref PrintStreamWriter
    inflows:
      message: /wps/result/

